let strArr: [string, number] = ["rohith", 1];
for (let i = 0; i < strArr.length; i++) {
    console.log(strArr[i]);
}

let numArr: any[] = [1, 2, "hii", 4, 5];
for (let i = 0; i < numArr.length; i++) {
    console.log(numArr[i]);
}

strArr[0]='19';

